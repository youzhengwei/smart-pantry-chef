# Botpress integration removed

The Botpress shopping-assistant has been removed from this project. The previous Botpress configuration and chatbot code were deprecated to simplify the codebase and remove the external dependency.

If you need a conversational assistant again later, reintroduce it as a separate, optional package and follow secure integration practices.
